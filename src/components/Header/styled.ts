import styled from 'styled-components';

export const StyledHeader = styled.nav`
  height: 100%;
  padding: 1em 0;
  background: #e6514b;
  font-weight: bold;
  color: #fff;

  ol {
    display: flex;
    justify-content: center;
    align-items: center;

    li {
      margin: 0 1em;
    }
  }
`;
